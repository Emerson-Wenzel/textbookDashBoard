'''
aws.py
1/11/2018
Emerson Wenzel

holds key and secret to access dynamoDB.
Included in .gitignore so it isn't available publicly
'''


aws_key_id = 'AKIAJPA5JGPE3M65GNTQ'
aws_key_secret = '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'



'''
    response_Dept_2 = table.query(
        IndexName = 'Dept_2-index',
        KeyConditionExpression = Key('Dept_1').eq(department)
    )

    # Convert query responses into a dataframe
    df = pd.DataFrame.from_dict(response_Dept_1['Items'] + \
                                response_Dept_2['Items'])
'''
